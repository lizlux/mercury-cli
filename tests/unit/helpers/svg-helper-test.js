import { svgHelper } from '../../../helpers/svg-helper';
import { module, test } from 'qunit';

module('Unit | Helper | svg helper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = svgHelper(42);
  assert.ok(result);
});
